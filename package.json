{
  "name": "cypressautomation",
  "version": "1.0.0",
  "description": "Descricao do cypressautomation",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "runchipr": "./node_modules/.bin/cypress run --spec 'cypress/integration/tests/firsttest.js' --browser chrome --headless"
  },
  "author": "Adriano Marqueze",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^6.6.0"
  }
}
